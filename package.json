{
  "name": "web-demo",
  "description": "Web demo",
  "version": "1.0.5",
  "license": "Apache-2.0",
  "dependencies": {
    "simplicite": "^1.0.5"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/simplicitesoftware/web-demo.git"
  },
  "scripts": {
    "build": "browserify index.js | uglifyjs -cm -o bundle.js",
    "watch": "watchify index.js --verbose -o bundle.js"
  }
}
